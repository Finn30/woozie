import 'package:flutter/material.dart';

class PricingRulesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Pricing Rules'),
      ),
      body: Center(
        child: Text('Here are the pricing rules.'),
      ),
    );
  }
}
