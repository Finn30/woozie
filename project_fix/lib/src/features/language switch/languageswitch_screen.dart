import 'package:flutter/material.dart';

class LanguageSwitchScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Language Switch'),
        centerTitle: true,
      ),
      body: Center(
        child: Text('Language Switch Screen'),
      ),
    );
  }
}
